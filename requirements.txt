# 修正后的requirements.txt（兼容Python 3.8+，版本均为官方存在的稳定版）
html2text==2025.4.15       # 版本存在，无需修改
lxml==6.0.2                # 版本存在，无需修改
mammoth==1.11.0            # 版本存在，无需修改
markdownify==1.2.2         # 版本存在，无需修改
numpy==2.2.6               # 修正：2.4.2不存在，改为最新稳定版（兼容Python 3.10+）
odfpy==1.4.1               # 版本存在，无需修改
olefile==0.47              # 版本存在，无需修改
openpyxl==3.1.5            # 版本存在，无需修改
pandas==2.2.2              # 修正：3.0.0不存在（最新稳定版2.2.2），且兼容numpy 2.2.6
Pillow==12.1.0             # 版本存在，无需修改
pillow_heif==1.2.0         # 版本存在，无需修改
pptx2md==2.0.6             # 版本存在，无需修改
readability_lxml==0.8.4.1  # 版本存在，无需修改
requests==2.32.5           # 修正：Requests→requests（Python包名小写）
setuptools==80.9.0         # 版本存在，无需修改（建议放宽为>=65.0.0，避免版本过新冲突）
xlrd==2.0.2                # 版本存在，无需修改